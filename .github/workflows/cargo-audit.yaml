name: Cargo Audit

on:
  workflow_call:
    inputs:
      working-directory:
        description: "Directory containing the Rust project (relative to repo root)"
        required: false
        type: string
        default: "."
      submodules:
        description: "Whether to checkout submodules ('true', 'recursive', or 'false')"
        required: false
        type: string
        default: "recursive"
      audit-args:
        description: "Additional arguments passed to cargo audit"
        required: false
        type: string
        default: ""
      deny-args:
        description: "Arguments passed to cargo deny check (leave empty to skip cargo deny)"
        required: false
        type: string
        default: ""
      runner:
        description: "GitHub Actions runner label"
        required: false
        type: string
        default: "ubuntu-latest"

permissions:
  contents: read

jobs:
  audit:
    name: Cargo Audit
    runs-on: ${{ inputs.runner }}
    defaults:
      run:
        working-directory: ${{ inputs.working-directory }}
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          submodules: ${{ inputs.submodules }}

      - name: Install Rust 1.95.0
        run: |
          rustup toolchain install 1.95.0 --profile minimal
          rustup default 1.95.0

      - name: Install cargo-audit
        run: cargo install cargo-audit --locked

      - name: Run cargo audit
        run: cargo audit ${{ inputs.audit-args }}

  deny:
    name: Cargo Deny
    if: inputs.deny-args != ''
    runs-on: ${{ inputs.runner }}
    defaults:
      run:
        working-directory: ${{ inputs.working-directory }}
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          submodules: ${{ inputs.submodules }}

      - name: Install Rust 1.95.0
        run: |
          rustup toolchain install 1.95.0 --profile minimal
          rustup default 1.95.0

      - name: Run cargo deny
        uses: EmbarkStudios/cargo-deny-action@34899fc7ba81ca6268d5947a7a16b4649013fea1 # v2.0.11
        with:
          command: check
          arguments: ${{ inputs.deny-args }}
          manifest-path: ${{ inputs.working-directory }}/Cargo.toml
